/** Static defaults used across the app. Centralized so the "house rules" live in one place. */
export const DEFAULT_MAFIA_COUNT = 2;
export const DEFAULT_DISCUSSION_SECONDS = 90;
export const MIN_PLAYERS = 5; // supports the default of 2 Mafia with a 2:1 civilian ratio
